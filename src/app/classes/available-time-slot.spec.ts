import { AvailableTimeSlot } from './available-time-slot';

describe('AvailableTimeSlot', () => {
  it('should create an instance', () => {
    expect(new AvailableTimeSlot()).toBeTruthy();
  });
});
